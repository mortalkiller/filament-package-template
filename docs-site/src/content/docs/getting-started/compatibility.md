---
title: Compatibility
description: Supported PHP, Laravel and Filament versions.
---

The generated package must document only compatibility that is declared in `composer.json` and exercised by CI.

Before the first stable release, replace this template text with the package's tested compatibility matrix.
