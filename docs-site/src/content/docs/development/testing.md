---
title: Testing
description: Run package tests and quality checks.
---

From the repository root:

```bash
composer install
composer check
```

Document package-specific JavaScript, workbench and browser commands here when they exist.

The CI matrix must cover every declared compatibility boundary, including minimum and latest allowed framework versions where practical.
